apiVersion: v1
data:
  package.json: |
    {
      "name": "claude-web-ui",
      "version": "5.0.0",
      "main": "server.js",
      "scripts": {
        "start": "node server.js"
      },
      "dependencies": {
        "express": "^4.18.2"
      }
    }
kind: ConfigMap
metadata:
  name: claude-webui-package
  namespace: claude-webui
