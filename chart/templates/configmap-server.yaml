apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "claude-webui.fullname" . }}-server
  labels:
    {{- include "claude-webui.labels" . | nindent 4 }}
data:
  server.js: |
{{ .Files.Get "files/server.js" | indent 4 }}
