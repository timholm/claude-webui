{{- if .Values.ssh.createSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.ssh.secretName }}
  labels:
    {{- include "claude-webui.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{ .Values.ssh.secretKey }}: {{ .Values.ssh.password | quote }}
{{- end }}
